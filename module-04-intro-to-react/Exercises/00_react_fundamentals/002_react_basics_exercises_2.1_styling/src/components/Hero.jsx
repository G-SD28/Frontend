import './Hero.css';

const Hero = () => {
  return (
    <section
      className='hero__heading'
      style={{
        cornerShape: 'squircle',
        borderRadius: '3rem',
      }}
    >
      Hero!
    </section>
  );
};
export default Hero;
